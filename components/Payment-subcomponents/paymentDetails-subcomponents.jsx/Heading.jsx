import React from 'react'

const Heading = ({ children }) => {
    return (
        <div>
            <h1 className="input-heading mb-2 text-black">{children}</h1>
        </div>
    )
}

export default Heading
